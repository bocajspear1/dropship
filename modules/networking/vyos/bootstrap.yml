---
- hosts: "{{ item }}"
  ansible_connection: network_cli
  tasks:
    - name: Setup hostname
      vyos_config:
        lines:
          - set system host-name {{ inventory_hostname }}
    - name: Setup interfaces
      debug:
        
      loop: "hostvars[{{ item }}]"